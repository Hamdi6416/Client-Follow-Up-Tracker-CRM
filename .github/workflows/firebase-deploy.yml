name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main  # Change if your deployment branch is different

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Match your project’s Node version

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Authenticate and deploy to Firebase
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "$FIREBASE_SERVICE_ACCOUNT" > firebase-key.json
          firebase deploy --project your-project-id --token "$(cat firebase-key.json | jq -r .token)"
